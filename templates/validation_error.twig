{# Used to render the validation exception messages Generated by the Command Bus Validation Middleware

   The frist Index is the Name of the Field/Property. Which passed through an optional map $aFieldMap  
   
   Vars 
   aFieldMap - Map Fieldnames in error message to Friendly Human names
   aErrors   - The validation error messages, the index the internal name of the field the value is another array with messages
   sBackBtnUrl - A Url to send user when press the back button
   sBackBtnText - Text to use in this back button
#}

{% extends '_base/_page-nav.twig' %}

{% block page_nav title %}

{% block page_title __(title) %}



{% block page_main %}

<p class="bg-danger">
    <ul>
    {% for sField, aError in aErrors %}
        <li>
            Field <strong>{{ attribute(aFieldMap,sField)|default(sField) }}</strong> has the following errors:
            <ul>
                {% for sError in aError %}
                <li>
                    {{ sError }}    
                </li>
                {% endfor %}    
            </ul>            
            
        </li>
    {% endfor %}
    </ul>
</p>

<hr />

<a class="btn btn-default" href="{{ sBackBtnUrl }}"> {{ sBackBtnText|default('Back to previous') }} </a>


{% endblock page_main %}